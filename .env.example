# =============================================================================
# Planning Poker - Configuration Minimale
# =============================================================================
# Copiez ce fichier vers .env et modifiez les valeurs

# -----------------------------------------------------------------------------
# Docker Hub
# -----------------------------------------------------------------------------
# Utilisateur qui a publié les images sur Docker Hub
DOCKER_USERNAME=borisngd

# Version des images à utiliser (latest, 1.0.0, etc.)
IMAGE_TAG=latest

# -----------------------------------------------------------------------------
# Base de données PostgreSQL
# -----------------------------------------------------------------------------
# Utilisateur de la base de données
POSTGRES_USER=poker_user

# ⚠️ IMPORTANT : Changez ce mot de passe !
# Utilisez un mot de passe fort et unique
POSTGRES_PASSWORD=ChangezCeMotDePasse123!

# Nom de la base de données
POSTGRES_DB=poker_planning

# -----------------------------------------------------------------------------
# Configuration publique
# -----------------------------------------------------------------------------
# URL publique où l'application sera accessible
# ⚠️ IMPORTANT pour le partage de sessions :
#   - Si vous utilisez localhost, les autres machines ne pourront pas rejoindre
#   - Utilisez votre IP locale (ex: http://192.168.1.100:8080) pour partager sur le réseau
#   - Utilisez votre domaine (ex: https://poker.votredomaine.com) pour internet
#
# Exemples :
#   - En local (pas de partage) : http://localhost:8080
#   - Réseau local (partage LAN) : http://192.168.1.100:8080
#   - Production (internet)      : https://poker.votredomaine.com
#
# Pour trouver votre IP locale :
#   - Windows : ipconfig (cherchez "Adresse IPv4")
#   - Linux/Mac : ip addr ou ifconfig (cherchez "inet")
PUBLIC_URL=http://localhost:8080

# Port d'écoute pour nginx (port public)
NGINX_PORT=8080

# -----------------------------------------------------------------------------
# Session
# -----------------------------------------------------------------------------
# Durée de vie d'une session en secondes
# 86400 = 24 heures
# 604800 = 7 jours
SESSION_TTL=86400

# -----------------------------------------------------------------------------
# Configuration avancée (optionnel)
# -----------------------------------------------------------------------------
# Niveau de log (error, warn, info, debug)
LOG_LEVEL=info
